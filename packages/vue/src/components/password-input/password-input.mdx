import { Canvas, Controls, Meta, Source } from "@storybook/addon-docs";
import * as PasswordInputStories from "./password-input.stories";
import PasswordInputCode from "./password-input.vue?raw";

<Meta of={PasswordInputStories} />

# Password Input

A password input component with a toggle button to show or hide the password.

## Usage

<Canvas of={PasswordInputStories.Default} />

<Controls />

## Installation

### 1. Copy and paste the following code into your project.

<Source language="html" code={PasswordInputCode} />
