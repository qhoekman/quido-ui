import { Canvas, Controls, Meta, Source } from '@storybook/addon-docs'
import * as BackToTopStories from './back-to-top.stories'
import BackToTopCode from './back-to-top.vue?raw'

<Meta of={BackToTopStories} />

# Back to Top

A component that displays a button to scroll back to the top of the page when the user has scrolled down.

## Usage

<Story of={BackToTopStories.Default} />

<Controls />

## Installation

### 1. Copy and paste the following code into your project.

<Source language="html" code={BackToTopCode} />
