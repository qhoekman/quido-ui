import { Canvas, Controls, Meta, Source } from "@storybook/addon-docs/blocks";
import * as SliderStories from "./slider.stories";
import SliderCode from "./slider.vue?raw";

<Meta of={SliderStories} />

# Slider

A slider component that allows users to select a single value from a range.

## Usage

<Canvas of={SliderStories.Default} />

<Controls />

## Installation

The Slider component uses Reka UI primitives under the hood. Make sure you have `reka-ui` installed:

\`\`\`bash
npm install reka-ui
\`\`\`

### 1. Copy and paste the following code into your project.

<Source language="html" code={SliderCode} />
