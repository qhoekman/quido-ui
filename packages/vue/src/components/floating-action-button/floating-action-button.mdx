import { Canvas, Controls, Meta, Source } from "@storybook/addon-docs";
import * as FloatingActionButtonStories from "./floating-action-button.stories";
import FloatingActionButtonCode from "./floating-action-button.vue?raw";

<Meta of={FloatingActionButtonStories} />

# Floating Action Button

A floating action button (FAB) is a circular button that is fixed to the bottom-right corner of the viewport. It provides quick access to a primary action.

## Usage

<Canvas of={FloatingActionButtonStories.Default} />

<Controls />

## Installation

### 1. Copy and paste the following code into your project.

<Source language="html" code={FloatingActionButtonCode} />
