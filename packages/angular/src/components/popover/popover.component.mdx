import * as PopoverStories from "./popover.component.stories";
import { Canvas, Meta, Source, ArgTypes, Controls } from "@storybook/addon-docs/blocks";
import PopoverCode from "!!raw-loader!./popover.component.ts";
import PopoverContentCode from "!!raw-loader!./popover-content.component.ts";
import PopoverTriggerCode from "!!raw-loader!./popover-trigger.directive.ts";

# Popover

<Meta of={PopoverStories} />
<Canvas of={PopoverStories.Default} />
<Controls of={PopoverStories.Default} />

## Installation

Copy the following code into your project.

> **popover.component.ts**
> <Source code={PopoverCode} language="typescript" />

> **popover-content.component.ts**
> <Source code={PopoverContentCode} language="typescript" />

> **popover-trigger.directive.ts**
> <Source code={PopoverTriggerCode} language="typescript" /> 
