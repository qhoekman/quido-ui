import * as ToasterStories from "./toaster.component.stories";
import ToasterStoriesCode from "!!raw-loader!./toaster.component.stories.ts";
import { Canvas, Meta, Source, ArgTypes, Controls } from "@storybook/addon-docs/blocks";
import ToasterCode from "!!raw-loader!./toaster.component.ts";
import ToastServiceCode from "!!raw-loader!./toast.service.ts";
import ToastComponentCode from "!!raw-loader!./toast.component.ts";
import ToastViewportComponentCode from "!!raw-loader!./toast-viewport.component.ts";
import ToastCloseComponentCode from "!!raw-loader!./toast-close.component.ts";
import ToastTitleComponentCode from "!!raw-loader!./toast-title.component.ts";
import ToastDescriptionComponentCode from "!!raw-loader!./toast-description.component.ts";
import ToastActionComponentCode from "!!raw-loader!./toast-action.component.ts";

# Toaster

<Meta of={ToasterStories} />
<Canvas of={ToasterStories.Default} />
<Controls of={ToasterStories.Default} />

## Installation

Copy the following code into your project.

> **toaster.component.ts**
>
> <Source code={ToasterCode} language="typescript" />

> **toast.service.ts**
>
> <Source code={ToastServiceCode} language="typescript" />

> **toast.component.ts**
>
> <Source code={ToastComponentCode} language="typescript" />

> **toast-action.component.ts**
>
> <Source code={ToastActionComponentCode} language="typescript" />

> **toast-viewport.component.ts**
>
> <Source code={ToastViewportComponentCode} language="typescript" />

> **toast-close.component.ts**
>
> <Source code={ToastCloseComponentCode} language="typescript" />

> **toast-title.component.ts**
>
> <Source code={ToastTitleComponentCode} language="typescript" />

> **toast-description.component.ts**
>
> <Source code={ToastDescriptionComponentCode} language="typescript" />

## Story source

<Source code={ToasterStoriesCode} language="typescript" />
