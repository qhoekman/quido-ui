import * as LineChartStories from "./line-chart.component.stories.ts";
import { Canvas, Meta, Source, ArgTypes, Controls } from "@storybook/addon-docs/blocks";

# Line Chart

<Meta of={LineChartStories} />
<Canvas of={LineChartStories.Default} />
<Controls of={LineChartStories.Default} />

## Installation

Install the ApexCharts library in your project.

```bash
npm install apexcharts ng-apexcharts --save
```

> line-chart.component.ts
> <Source of={LineChartStories.Default} />
