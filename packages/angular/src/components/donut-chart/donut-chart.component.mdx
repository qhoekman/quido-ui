import * as DonutChartStories from "./donut-chart.component.stories.ts";
import { Canvas, Meta, Source, ArgTypes, Controls } from "@storybook/addon-docs/blocks";

# Donut Chart

<Meta of={DonutChartStories} />
<Canvas of={DonutChartStories.Default} />
<Controls of={DonutChartStories.Default} />

## Installation

Install the ApexCharts library in your project.

```bash
npm install apexcharts ng-apexcharts --save
```

> donut-chart.component.ts
> <Source of={DonutChartStories.Default} />
