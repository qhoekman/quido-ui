import * as BarChartStories from "./bar-chart.component.stories.ts";
import { Canvas, Meta, Source, ArgTypes, Controls } from "@storybook/addon-docs/blocks";

# Bar Chart

<Meta of={BarChartStories} />
<Canvas of={BarChartStories.Default} />
<Controls of={BarChartStories.Default} />

## Installation

Install the ApexCharts library in your project.

```bash
npm install apexcharts ng-apexcharts --save
```

> bar-chart.component.ts
> <Source of={BarChartStories.Default} />
