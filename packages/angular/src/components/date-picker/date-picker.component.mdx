import * as DatePickerStories from './date-picker.component.stories';
import { Canvas, Meta, Source, ArgTypes, Controls } from '@storybook/addon-docs/blocks';
import DatePickerCode from '!!raw-loader!./date-picker.component.ts';
import CalendarCode from '!!raw-loader!../calendar/calendar.component.ts';
import PopoverCode from '!!raw-loader!../popover/popover.component.ts';
import PopoverTriggerCode from '!!raw-loader!../popover/popover-trigger.directive.ts';

# Date Picker

<Meta of={DatePickerStories} />
<Canvas of={DatePickerStories.Default} />
<Controls of={DatePickerStories.Default} />

## Installation

Copy the following code into your project.

> **popover.component.ts**
> <Source code={PopoverCode} language="typescript" />

> **popover-trigger.directive.ts**
> <Source code={PopoverTriggerCode} language="typescript" />

> **calendar.component.ts**
> <Source code={CalendarCode} language="typescript" />

> **date-picker.component.ts**
> <Source code={DatePickerCode} language="typescript" />
