import * as AreaChartStories from "./area-chart.component.stories.ts";
import { Canvas, Meta, Source, ArgTypes, Controls } from "@storybook/addon-docs/blocks";

# Area Chart

<Meta of={AreaChartStories} />
<Canvas of={AreaChartStories.Default} />
<Controls of={AreaChartStories.Default} />

## Installation

Install the ApexCharts library in your project.

```bash
npm install apexcharts ng-apexcharts --save
```

> area-chart.component.ts
> <Source of={AreaChartStories.Default} />
