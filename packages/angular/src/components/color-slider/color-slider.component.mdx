import * as ColorSliderStories from "./color-slider.component.stories";
import { Canvas, Meta, Source, ArgTypes, Controls } from "@storybook/addon-docs/blocks";
import ColorSliderCode from "!!raw-loader!./color-slider.component.ts";

# Color Slider

<Meta of={ColorSliderStories} />
<Canvas of={ColorSliderStories.Default} />
<Controls of={ColorSliderStories.Default} />

## Installation

Copy the following code into your project.

> **color-slider.component.ts**
>
> <Source code={ColorSliderCode} language="typescript" />
