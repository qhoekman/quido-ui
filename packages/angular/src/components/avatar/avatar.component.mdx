import * as AvatarStories from "./avatar.component.stories";
import { Canvas, Meta, Source, ArgTypes, Controls } from "@storybook/addon-docs/blocks";
import AvatarCode from "!!raw-loader!./avatar.component.ts";
import AvatarImageCode from "!!raw-loader!./avatar-image.component.ts";
import AvatarFallbackCode from "!!raw-loader!./avatar-fallback.component.ts";

# Avatar

<Meta of={AvatarStories} />
<Canvas of={AvatarStories.Default} />
<Controls of={AvatarStories.Default} />

## Installation

Copy the following code into your project.

> **avatar.component.ts**
> <Source code={AvatarCode} language="typescript" />

> **avatar-image.component.ts**
> <Source code={AvatarImageCode} language="typescript" />

> **avatar-fallback.component.ts**
> <Source code={AvatarFallbackCode} language="typescript" />
