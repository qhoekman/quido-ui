import * as PieChartStories from "./pie-chart.component.stories.ts";
import { Canvas, Meta, Source, ArgTypes, Controls } from "@storybook/addon-docs/blocks";

# Pie Chart

<Meta of={PieChartStories} />
<Canvas of={PieChartStories.Default} />
<Controls of={PieChartStories.Default} />

## Installation

Install the ApexCharts library in your project.

```bash
npm install apexcharts ng-apexcharts --save
```

> pie-chart.component.ts
> <Source of={PieChartStories.Default} />
