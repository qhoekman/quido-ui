import * as RadarChartStories from "./radar-chart.component.stories.ts";
import { Canvas, Meta, Source, ArgTypes, Controls } from "@storybook/addon-docs/blocks";

# Radar Chart

<Meta of={RadarChartStories} />
<Canvas of={RadarChartStories.Default} />
<Controls of={RadarChartStories.Default} />

## Installation

Install the ApexCharts library in your project.

```bash
npm install apexcharts ng-apexcharts --save
```

> radar-chart.component.ts
> <Source of={RadarChartStories.Default} />
