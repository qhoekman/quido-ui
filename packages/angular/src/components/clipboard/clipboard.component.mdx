import * as ClipboardStories from "./clipboard.component.stories.ts";
import { Canvas, Meta, Source, ArgTypes, Controls } from "@storybook/addon-docs/blocks";

# Clipboard Component

<Meta of={ClipboardStories} />
<Canvas of={ClipboardStories.Default} />
<Controls of={ClipboardStories.Default} />

## Installation

Install the necessary Angular CDK library in your project.

```bash
npm install @angular/cdk --save
```

> clipboard.component.ts
> <Source of={ClipboardStories.Default} />
