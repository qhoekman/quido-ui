import { Canvas, Controls, Meta, Source } from '@storybook/addon-docs/blocks';

import * as Stories from './radio-group.stories.tsx';
import Code from './radio-group?raw';

<Meta of={Stories} />

# Usage

<Canvas of={Stories.Default} />

<Controls />

# Installation

## 1. Install the following dependencies

<Source language="bash" code={`npm install @radix-ui/react-radio-group`} />

See the [Radix](https://www.radix-ui.com/primitives/docs/components/radio-group) documentation for more details.

## 2. Copy and paste the following code into your project.

<Source language="tsx" code={Code} />

## 3. Usage in form

Please note that the component will only render native `input` elements when used within a `form` element.
