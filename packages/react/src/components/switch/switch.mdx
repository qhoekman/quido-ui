import { Canvas, Controls, Meta, Source } from '@storybook/blocks';

import * as Stories from './switch.stories.tsx';
import Code from './switch?raw';

<Meta of={Stories} />

# Usage

<Canvas of={Stories.Default} />

<Controls />

# Installation

## 1. Install the following dependencies

<Source language="bash" code={`npm install @radix-ui/react-switch`} />

See the [Radix](https://www.radix-ui.com/primitives/docs/components/switch) documentation for more details.

## 2. Copy and paste the following code into your project.

<Source language="tsx" code={Code} />

## 3. Usage in form

Please note that the component will only render a native `input` when used within a `form` element.
