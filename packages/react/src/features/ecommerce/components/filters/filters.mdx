import { Canvas, Controls, Meta, Source } from "@storybook/blocks";
import * as Stories from "./filters.stories";
import Code from "./filters.tsx?raw";
import Hooks from "./useFilterStore.tsx?raw";

<Meta of={Stories} />

# Usage

<Canvas of={Stories.Default} />

<Controls />

# Installation

## 1. Install the following packages.

<Source language="bash" code="npm install zustand" />

## 2. Add the following components to your project.

- Range
- Checkbox
- RadioGroup

## 3. Copy and paste the following code into your project.

<Source language="tsx" code={Hooks} />

## 4. Copy and paste the following code into your project.

<Source language="tsx" code={Code} />

# Details

The filters will be set via the hash in the URL via the persistance middleware of Zustand. You may want to swap this implementation out for a different one if you want to use a different state management library.
