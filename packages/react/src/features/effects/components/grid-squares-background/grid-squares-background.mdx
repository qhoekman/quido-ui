import { Canvas, Source, Controls, Meta } from "@storybook/addon-docs/blocks";
import * as GridSquaresBackgroundStories from "./grid-squares-background.stories";
import GridSquaresBackgroundCode from "./grid-squares-background.tsx?raw";

<Meta of={GridSquaresBackgroundStories} />

# Usage

<Canvas of={GridSquaresBackgroundStories.Default} />

<Controls />

# Installation

## 1. Install the following dependencies

<Source language="bash" code={`npm install three`} />

## 2. Copy and paste the following code into your project.

<Source language="tsx" code={GridSquaresBackgroundCode} />
