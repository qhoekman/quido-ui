import { Canvas, Source, Controls, Meta } from "@storybook/addon-docs/blocks";
import * as AbstractCirclesBackgroundStories from "./abstract-circles-background.stories";
import AbstractCirclesBackgroundCode from "./abstract-circles-background.tsx?raw";

<Meta of={AbstractCirclesBackgroundStories} />

# Usage

<Canvas of={AbstractCirclesBackgroundStories.Default} />

<Controls />

# Installation

## 1. Copy and paste the following code into your project.

<Source language="tsx" code={AbstractCirclesBackgroundCode} />
