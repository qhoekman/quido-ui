import { Canvas, Source, Controls, Meta } from "@storybook/addon-docs/blocks";
import * as FireflyOrbsBackgroundStories from "./firefly-orbs-background.stories";
import FireflyOrbsBackgroundCode from "./firefly-orbs-background.tsx?raw";

<Meta of={FireflyOrbsBackgroundStories} />

# Usage

<Canvas of={FireflyOrbsBackgroundStories.Default} />

<Controls />

# Installation

## 1. Copy and paste the following code into your project.

<Source language="tsx" code={FireflyOrbsBackgroundCode} />
