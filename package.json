{
  "name": "quido-ui",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "dev:react": "lerna run dev --scope @quido-ui/react",
    "dev:angular": "lerna run dev --scope @quido-ui/angular",
    "build:host": "lerna run build --scope @quido-ui/host",
    "build:angular": "lerna run build --scope @quido-ui/angular",
    "build:react": "lerna run build --scope @quido-ui/react",
    "build:vue": "lerna run build --scope @quido-ui/vue",
    "build": "npm run build:host && npm run build:angular && npm run build:react && npm run build:vue",
    "clean": "rm -rf packages/*/storybook-static"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^8.0.2"
  }
}
