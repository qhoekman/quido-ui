{
  "name": "quido-ui",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "dev:react": "lerna run dev --scope @quido-ui/react",
    "dev:angular": "lerna run dev --scope @quido-ui/angular",
    "build:host": "lerna run build --scope @quido-ui/host",
    "build:components": "lerna run build --scope '{*/angular, */react, */vue}'",
    "build": "npm run build:host && npm run build:components",
    "clean": "rm -rf packages/*/storybook-static"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^8.0.2"
  }
}
